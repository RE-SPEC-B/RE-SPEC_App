import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const MentorDetailScreen = ({ navigations }) => {
  return (
    <SafeAreaView>
      <Text>MentorDetail</Text>
    </SafeAreaView>
  );
};

export default MentorDetailScreen;
