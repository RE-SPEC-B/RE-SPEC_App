import { Layout as L, ScrollLayout as SL } from "./Layout";
import Section from "./Section";

export const Layout = Object.assign(L, {
  Section: Section,
});

export const ScrollLayout = Object.assign(SL, {
  Section: Section,
});
